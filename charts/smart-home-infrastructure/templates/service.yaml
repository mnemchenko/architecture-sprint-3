apiVersion: v1
kind: Service
metadata:
  name: {{ include "smart-home-infrastructure.fullname" . }}
  labels:
    {{- include "smart-home-infrastructure.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smart-home-infrastructure.selectorLabels" . | nindent 4 }}
